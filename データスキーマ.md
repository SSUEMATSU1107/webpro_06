# /yamanote
sequenceDiagram
  autonumber
  Webブラウザ ->> Webサーバ: GET /yamanote (一覧ページ要求)
  Webサーバ ->> DB(配列データ): 駅データ要求
  DB(配列データ) -->> Webサーバ: 駅データ配列
  Webサーバ ->> Webブラウザ: EJSで生成した HTML

# /yamanote/0
sequenceDiagram
  autonumber
  Webブラウザ ->> Webサーバ: GET /yamanote/:number
  Webサーバ ->> DB(配列データ): 該当indexの駅データ取得
  DB(配列データ) -->> Webサーバ: 駅データ（1件）
  Webサーバ ->> Webブラウザ: 詳細ページ(HTML)

# /sweets_add?name=◯◯
sequenceDiagram
  autonumber
  Webブラウザ ->> Webサーバ: GET /sweets_add?name=◯◯
  Webサーバ ->> DB(配列データ): 新規データを追加
  DB(配列データ) -->> Webサーバ: 追加後の配列
  Webサーバ ->> Webブラウザ: 一覧ページを再表示

# 全体の流れ
sequenceDiagram
  autonumber
  Webブラウザ ->> Webサーバ: Webページ要求
  Webサーバ ->> Webブラウザ: HTML + CSS + JS

  Webブラウザ ->> Webサーバ: GET /training (一覧要求)
  Webサーバ ->> DB(配列データ): トレーニング一覧取得
  DB(配列データ) -->> Webサーバ: 一覧データ
  Webサーバ ->> Webブラウザ: 一覧EJS → HTML生成

  Webブラウザ ->> Webサーバ: GET /training/:number (詳細要求)
  Webサーバ ->> DB(配列データ): 指定IDデータ取得
  DB(配列データ) -->> Webサーバ: 詳細データ
  Webサーバ ->> Webブラウザ: 詳細EJS → HTML生成
