# 開発者向け仕様書（仮）: 筋力トレーニング管理システム

## 1. 概要

本システムは、ユーザーが自身の**筋力トレーニングのメニュー**を管理し、**トレーニングの実施記録**を記録・確認するためのWebアプリケーションである。

## 2. データ構造

システム内で扱う主なデータ構造を以下に示す。

### 2.1. トレーニングメニュー (Menu)
| フィールド名 | データ型 | 説明 |
| :--- | :--- | :--- |
| `menu_id` | Integer | メニューの一意なID（主キー） |
| `name` | String | トレーニングメニュー名（例: スクワット, ベンチプレス） |
| `target_muscle` | String | 対象となる主な筋肉（例: 大腿四頭筋, 大胸筋） |
| `description` | Text | トレーニングの詳細な説明や注意点 |

### 2.2. 実施記録 (Record)
| フィールド名 | データ型 | 説明 |
| :--- | :--- | :--- |
| `record_id` | Integer | 記録の一意なID（主キー） |
| `menu_id` | Integer | 実施したメニューのID（Menuへの外部キー） |
| `date` | Date | 実施日 |
| `weight` | Float | 使用した重量（kg） |
| `reps` | Integer | 繰り返し回数（reps） |
| `sets` | Integer | セット数（sets） |
| `note` | Text | 記録に関するメモ |

## 3. HTTPメソッドとリソース名一覧

主なリソースと、それに対するCRUD操作（作成、読み取り、更新、削除）を行うためのHTTPメソッドを定義する。

| HTTPメソッド | リソース名 | 機能 |
| :--- | :--- | :--- |
| `GET` | `/menus` | メニュー**一覧**の取得 |
| `POST` | `/menus` | 新しいメニューの**作成** |
| `GET` | `/menus/{menu_id}` | 特定メニューの**詳細**取得 |
| `PUT/PATCH` | `/menus/{menu_id}` | 特定メニューの**更新** |
| `DELETE` | `/menus/{menu_id}` | 特定メニューの**削除** |
| `GET` | `/records` | 実施記録**一覧**の取得 |
| `POST` | `/records` | 新しい実施記録の**作成** |
| `PUT/PATCH` | `/records/{record_id}` | 特定記録の**更新** |
| `DELETE` | `/records/{record_id}` | 特定記録の**削除** |

## 4. ページ遷移図（Mermaidによる簡易表現）

主な画面間の遷移を下書きとして表現する。

```mermaid
graph LR
    A[トップページ/記録一覧] --> B(記録詳細);
    A --> C(メニュー一覧);
    A --> D(新規記録追加);
    B --> E(記録編集);
    C --> F(メニュー詳細);
    C --> G(新規メニュー追加);
    F --> H(メニュー編集);
    D --> A;
    E --> B;
    G --> C;
    H --> F;